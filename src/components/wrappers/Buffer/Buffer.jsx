import React from 'react';
import styles from '../../../styles/layouts/Buffer.module.scss';

const Buffer = props => {
  return <div className={styles.cardWrapper} {...props} />;
};

export default Buffer;
